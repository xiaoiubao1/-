v1.4.0 新增多学期、单双周、自定义作息、今日课程、课程搜索和教务网页 / CSV / HTML 导入。详见[课表与教务导入使用说明](docs/TIMETABLE.md)。

# 随心记（Android）

一个以 **随手记录 + 紧凑周课表 + 提醒 + 本地备份 + 桌面小组件** 为核心的原生 Android 应用。当前代码版本 **v1.3.3**（versionCode 7）；Release 下载页可能仍为上一个已发布版本。

## v1.3.3 修复

- 恢复备份前完整校验清单、字段类型、图片路径和课程时间；使用独立媒体目录与数据库事务，失败保留原记录、图片和提醒。缺图会明确提示。
- CSV 兼容 UTF-8 BOM、跨行字段、逗号和引号；保留毫秒时间、创建时间和图片 URI。CSV 不打包图片，迁移到另一台设备请使用完整备份。
- 提醒改用系统闹钟，只传记录 ID 和时间，通知触发时读取数据库，长文本不会进入 WorkManager 的大小受限数据包。
- “我的”显示通知和准时提醒权限状态。未允许准时提醒时使用可能延迟的系统提醒；重启、升级、时间/时区变化及返回应用时重建提醒。强行停止、关机及厂商省电限制仍可能阻止通知。
- 日期选择正确转换 UTC 与本地日期；同格多门课程可逐一选择，跨课节课程出现在相交课节。
- 编辑草稿放入 ViewModel，旋转屏幕时保留；系统结束进程或退出应用前仍应点击保存。
- 图片在后台采样解码并缓存，小组件更新做短暂合并；通知、课程和小组件使用独立的点击标识。
- 关闭 Android 系统自动备份与设备迁移的数据范围。手动 `.suixinji` 是未加密 ZIP，请妥善保管；恢复后旧媒体暂保留在应用私有目录，避免中断时误删原图。
- 增加备份/CSV/日期/长文本提醒等回归测试；CI 执行单元测试、Android Lint、Release 构建和签名验证。`workflow_dispatch` 在 main 使用原正式签名，其余分支使用临时测试签名。

提交消息含 `[skip release]` 时，main 仍构建正式签名 APK 并保存为 Actions artifact，但不发布 GitHub Release。修复分支 `fix/**` 自动验证；其临时签名 APK 不能覆盖安装正式版本。

## v1.3.2 优化

- ⭕ **悬浮圆角底栏**：底部导航改成左右留白的胶囊形玻璃容器，不再铺满整屏
- 🫧 **高透明玻璃**：自定义壁纸时底栏继续维持约 10% 不透明度，并增加轻量描边
- 🎯 **圆润选中态**：课表 / 随心记 / 我的当前项使用独立圆角胶囊高亮
- 📐 **悬浮间距**：底栏与左右边缘、系统导航区域保留呼吸空间
- 🔢 **版本**：versionCode 6 / versionName 1.3.2

## v1.3.1 优化

- ⏱️ **全新时间选择**：不再调用 Android 原生 `DatePickerDialog / TimePickerDialog`
- ⚡ **快捷日期**：今天、明天、后天和近 10 天可以直接点选，远期日期可展开 Compose Material 3 日历
- 🕐 **快捷时间**：常用时间一键选择，也可按小时 + 5 分钟粒度精确选择
- 📚 **课程时间更快**：1～8 节课可以直接套用标准时间，开始/结束时间仍支持自定义
- 🪟 **更通透的玻璃层**：有自定义壁纸时，内容卡片约为 16%～28% 不透明度，减少白色遮挡
- 🌫️ **轻雾化背景**：降低壁纸模糊和白色覆盖，不再把背景“洗白”
- 🧭 **透明底部导航**：取消默认 Material NavigationBar 的高不透明背景，改用约 10% 的自绘透明层
- 📱 **Edge-to-edge**：状态栏和系统导航栏改为透明，Android 10+ 关闭导航栏强制对比底色
- 🔢 **版本**：versionCode 5 / versionName 1.3.1

## v1.3.0 新增

- 🧩 **桌面小组件 DIY**：可自选背景图片、背景色、文字颜色模式、强调色、透明度与磨砂覆盖
- 🔄 **小组件即时刷新**：修改 DIY 设置后，桌面上已添加的小组件会立即刷新
- 📅 **一屏周课表**：周一到周日与 1～8 节课压缩在同一屏内，无需横向滑动即可快速扫完整周
- 🎨 **课程色块**：课程名称以紧凑色块显示，空间允许时同时显示教室
- ➕ **格子直达编辑**：点击课表空白格按对应星期/课节添加课程，点击已有课程进入编辑
- 🖼️ **统一 APP 图标**：使用用户指定的灰发蓝眼草帽角色裁剪图作为唯一启动图标
- 💾 **备份增强**：`.suixinji` 完整备份同时保存小组件 DIY 设置和可读取的小组件背景图片

> Android 经典 RemoteViews 小组件无法在所有品牌桌面上提供真正的系统级实时背景模糊，因此桌面小组件仍采用“自定义图片 + 半透明叠层 + 颜色/透明度控制”的兼容方案；APP 本体则使用更低不透明度的 Compose 玻璃层。

## 已有功能

- 📝 记录标题、内容、地点、时间与图片
- 🔎 搜索标题 / 内容 / 地点，筛选全部 / 待办 / 已完成
- 🔔 事件提醒、通知测试
- 📚 周课程表，课程老师 / 教室 / 备注
- ⏰ 课程开始提醒：上课时或提前 5 / 10 / 15 / 30 / 60 分钟
- 📥 CSV / JSON / TXT 导入
- 📤 CSV 导出
- 💾 `.suixinji` 完整备份与恢复
- ☀️ 浅色 / 🩶 灰色默认背景
- 🖼️ 用户主动开启后可选择自定义 APP 背景
- 🫧 可调应用雾化强度
- 🔒 SQLite 本地存储，无账号、无服务器依赖

## 桌面小组件 DIY

进入 **我的 → 桌面小组件 DIY**：

- 选择或移除小组件背景图片
- 选择背景颜色
- 文字颜色：自动 / 深色 / 浅色
- 选择强调色
- 调整背景透明度
- 开关磨砂覆盖

桌面添加方式：长按桌面空白处 → **小组件** → **随心记** → **随心记概览**。

## 紧凑周课表

课表页固定展示 **周一～周日 × 1～8 节**。课程名称最多两行，点击空白格新增课程，点击已有课程进入编辑。v1.3.1 的课程编辑页还增加了 **1～8 节标准时间快捷套用**。

## 数据导出与完整备份

进入 **我的 → 数据**：

- **导入**：CSV、JSON、TXT
- **CSV**：导出全部随心记记录
- **备份**：生成 `.suixinji` 完整备份
- **恢复**：覆盖恢复记录、课程、主题、背景、小组件 DIY 设置和可读取图片，并重新建立提醒

## 正式签名与覆盖升级

Android 覆盖安装要求包名相同，并且新旧 APK 使用同一套正式签名密钥。正式签名材料只通过 GitHub Actions Secret `ANDROID_SIGNING_BUNDLE` 注入，公开仓库不保存私钥。

详细说明见 [`docs/SIGNING.md`](docs/SIGNING.md)。

## 下载最新版 APK

网站可以长期使用固定地址：

```text
https://github.com/xiaoiubao1/-/releases/latest/download/suixinji.apk
```

`main` 更新后会自动构建正式 Release APK、执行 `apksigner verify`，并创建新的 GitHub Release。

## 技术栈

- Kotlin 2.3.21
- Jetpack Compose / Material 3
- Compose BOM 2026.06.00
- Android Gradle Plugin 8.13.2
- Gradle 8.13 / JDK 17
- compileSdk / targetSdk 36
- minSdk 26（Android 8.0+）
- WorkManager 2.11.2
- SQLiteOpenHelper
- RemoteViews + AppWidgetProvider

## 数据位置

- 记录与课程：应用私有 SQLite 数据库 `suixinji.db`
- 个性化设置：应用私有 SharedPreferences
- 恢复的图片：应用私有 `filesDir/restored_media`

卸载应用会清除本地数据，建议定期创建 `.suixinji` 备份并保存到其他位置。
